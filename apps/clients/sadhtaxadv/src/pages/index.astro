---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { ButtonDemo } from '../components/ButtonDemo';

const services = await getCollection('services');
const sortedServices = services.sort((a, b) => a.data.order - b.data.order);
---

<Layout
	title="Professional Consulting Services | Your Consulting Firm"
	description="Expert consulting services in business strategy, tax advisory, and financial planning."
>
	<!-- Hero Section -->
	<section class="bg-gradient-to-r from-primary-600 to-primary-800 text-white">
		<div class="container mx-auto px-4 py-20 text-center">
			<h1 class="text-5xl md:text-6xl font-bold mb-6">
				Professional Consulting Services
			</h1>
			<p class="text-xl md:text-2xl mb-8 max-w-3xl mx-auto">
				Expert guidance to help your business grow, optimize operations, and achieve financial success.
			</p>
			<a
				href="#services"
				class="inline-block bg-white text-primary-600 px-8 py-3 rounded-lg font-semibold hover:bg-gray-100 transition"
			>
				Explore Our Services
			</a>
		</div>
	</section>

	<!-- Services Section -->
	<section id="services" class="py-20">
		<div class="container mx-auto px-4">
			<h2 class="text-4xl font-bold text-center mb-12">Our Services</h2>
			<div class="grid md:grid-cols-3 gap-8">
				{sortedServices.map((service) => (
					<div class="bg-white p-8 rounded-lg shadow-lg hover:shadow-xl transition border border-gray-200">
						<h3 class="text-2xl font-bold mb-4 text-primary-600">
							{service.data.title}
						</h3>
						<p class="text-gray-600 mb-4">
							{service.data.description}
						</p>
						<a
							href={`/services/${service.slug}`}
							class="text-primary-600 font-semibold hover:text-primary-700"
						>
							Learn More →
						</a>
					</div>
				))}
			</div>
		</div>
	</section>

	<!-- shadcn/ui Demo Section -->
	<section class="py-20 border-t">
		<div class="container mx-auto px-4">
			<h2 class="text-4xl font-bold text-center mb-4">shadcn/ui Components</h2>
			<p class="text-center text-muted-foreground mb-12 max-w-2xl mx-auto">
				Built with beautiful, accessible components powered by shadcn/ui and Radix UI
			</p>
			<div class="max-w-4xl mx-auto bg-card border rounded-lg p-8">
				<h3 class="text-xl font-semibold mb-6">Button Variants</h3>
				<ButtonDemo client:load />
			</div>
		</div>
	</section>

	<!-- CTA Section -->
	<section class="bg-muted py-20">
		<div class="container mx-auto px-4 text-center">
			<h2 class="text-4xl font-bold mb-6">Ready to Get Started?</h2>
			<p class="text-xl text-gray-600 mb-8 max-w-2xl mx-auto">
				Contact us today to learn how we can help your business succeed.
			</p>
			<a
				href="/contact"
				class="inline-block bg-primary-600 text-white px-8 py-3 rounded-lg font-semibold hover:bg-primary-700 transition"
			>
				Contact Us
			</a>
		</div>
	</section>
</Layout>
